# 🧠 AI AGENTS SKILLS - Visaginas360 AGI Project

## 📋 Общие правила для ВСЕХ агентов

### 1. Shared Context
- **ВСЕГДА** регистрируйся при старте: `POST /agents/register`
- **ВСЕГДА** отправляй heartbeat каждые 5 минут
- **ЧИТАЙ** общий контекст перед работой: `GET /context`
- **ПИШИ** результаты в общий контекст

### 2. Коммуникация между агентами
```
Shared Context API: http://localhost:5020
Gemini Agent: http://localhost:5050
OpenAI Agent: http://localhost:5052
Orchestrator: http://localhost:5025
```

---

## 🎯 MULTI-AGENT ORCHESTRATOR

### Назначение: Параллельное выполнение сложных задач с обучением

### Как работает:
1. **Декомпозиция** - разбивает задачу на подзадачи
2. **Распределение** - выбирает лучшего агента для каждой
3. **Параллельное выполнение** - запускает все сразу
4. **Синтез** - объединяет результаты в один ответ
5. **Обучение** - запоминает успехи и ошибки

### Endpoints:
```
POST /orchestrate - сложная задача (автоматическая декомпозиция)
POST /simple - простая задача (один агент)
GET /skills - выученные скиллы
GET /history - история выполнения
GET /errors - паттерны ошибок
```

### Пример:
```python
response = requests.post("http://localhost:5025/orchestrate", json={
    "task": "Создай план развития AGI: анализ, улучшения, риски"
})
# Автоматически разбивается на части и выполняется параллельно
```

---

## 🤖 GEMINI AGENT

**Роль:** Аналитик и генератор контента  
**Ключевые слова:** `напиши, создай, текст, отчёт, креатив, идеи`

---

## 🧠 OPENAI AGENT

**Роль:** Стратег и логик  
**Ключевые слова:** `посчитай, логика, код, анализ, стратегия`

---

## 📊 WORKFLOW: Сложная задача

```
User → Orchestrator → Decompose → Parallel Execution → Synthesize → Learn
```

---

## 🧠 ПРИНЦИП ОБУЧЕНИЯ

- **Успехи** → увеличивают счётчик скиллов агента
- **Ошибки** → записываются для анализа
- **История** → хранит последние 100 задач

Со временем оркестратор лучше выбирает агентов для задач.

---

Обновлено: 2026-01-22
